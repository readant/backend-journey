---
description: "Java 编码规范：命名、Lombok、日志"
globs: ["**/*.java"]
---

# Java 编码规范

## 1. 命名约定

- **类/接口/枚举**：`UpperCamelCase`（如 `UserService`）
- **方法/变量**：`lowerCamelCase`（如 `getUserById`）
- **常量**：`UPPER_SNAKE_CASE`（如 `MAX_RETRY_COUNT`）
- **包名**：全小写，如 `com.readant.cms.service`

## 2. Lombok 强制规范

- **实体类（Entity）**：`@Data` + `@Builder`（推荐）
- **值对象（VO/DTO）**：`@Data` 或 `@Value`
- **日志**：每个 Service/Controller 必须加 `@Slf4j`，禁止手动创建 Logger
- **禁止**：手写 getter/setter、toString、equals、hashCode、构造方法

## 3. 日志规范

- 使用占位符：`log.info("用户 {} 登录成功", userId);`
- 禁止字符串拼接：~~`log.info("用户 " + userId + " 登录成功")`~~
- 异常必须打印堆栈：`log.error("业务异常", e);`
- 级别使用：
  - `ERROR`：系统异常、未预期的错误
  - `WARN`：业务异常、可恢复的问题
  - `INFO`：关键业务节点（登录、下单、支付）
  - `DEBUG`：调试信息，生产环境关闭
