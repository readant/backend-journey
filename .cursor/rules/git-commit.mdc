---
description: "Git 提交规范。AI 每次提交代码时必须遵守此规范。"
globs: ""
alwaysApply: true
---

# Git 提交规范

## 零. 分支策略（最高优先级，GitHub Flow）

> **禁止直接在 `main` 上提交。** `main` 永远保持可发布、干净，任何改动都必须走分支合回。

### 分支模型

```
main（唯一主干，永远可发布）
  │
  ├── feature/cms-xxx     ← 新功能：从 main 拉出
  ├── fix/xxx             ← 修复：从 main 拉出
  └── docs/learning       ← 文档：从 main 拉出
```

### 强制流程

```bash
# 1. 确保 main 最新
git checkout main && git pull

# 2. 拉取功能分支（命名含类型前缀）
git checkout -b feature/xxx

# 3. 在功能分支上开发，按规范拆分提交（见下文）

# 4. 合回 main：用 --no-ff 保留分支历史
git checkout main
git merge --no-ff feature/xxx -m "Merge branch 'feature/xxx' into main"

# 5. 推送 main，删除已合并的功能分支
git push origin main
git branch -d feature/xxx
```

### 分支命名

- `feature/<描述>`：新功能
- `fix/<描述>`：修复
- `docs/<描述>`：文档
- `refactor/<描述>`：重构

### 禁止行为

- ❌ 直接在 `main` 上 `git commit`
- ❌ 在 `main` 上 `git add .` + 一次性提交
- ❌ 提交后不推送、不清理已合并的功能分支
- ❌ 功能分支长期不合并（应尽快合回，避免与 main 大幅分叉）
- ❌ 在无关的前端仓库（如 frontend-journey）执行 git 命令（必须用 `working_directory` 指到本仓库）

## 一. 基本原则

1. **按阶段/功能拆分提交** — 禁止批量统一提交，每个提交只包含一个逻辑变更
2. **可追溯** — 每个提交应能独立理解，方便回滚到任意历史点
3. **中文描述** — description 部分使用简体中文

## 提交格式

```
<type>(<scope>): <description>
```

### type（必填）

| type | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(phase2): 管理员 CRUD` |
| `fix` | Bug 修复 | `fix: 修复登录接口空指针异常` |
| `chore` | 基础设施/配置 | `chore: 初始化项目基础设施` |
| `docs` | 文档 | `docs: 更新 README` |
| `refactor` | 重构 | `refactor: 提取公共分页方法` |
| `test` | 测试 | `test: 添加 AdminService 单元测试` |

### scope（可选）

| scope | 说明 |
|-------|------|
| `infrastructure` | 项目基础设施（.gitignore、规则文件等） |
| `phase1` ~ `phase4` | 对应开发阶段 |
| `cms` / `ecommerce` / `community` / `saas` | 对应项目 |

### description（必填）

- 使用简体中文
- 简洁概括变更内容，不超过 50 字
- 如有必要，可在提交信息正文中补充详细说明

## 提交示例

```
# 新功能提交
feat(phase1): 骨架搭建 - Maven + Spring Boot + 公共基础 + Health API

# 修复提交
fix: 修复文章分页查询总数不准确的问题

# 文档提交
docs: 更新项目 README 和速查卡片

# 重构提交
refactor(cms): 提取公共树形结构查询方法到基类

# 测试提交
test: 添加 AdminServiceImpl 单元测试覆盖登录分支
```

## 禁止行为

- ❌ 禁止 `git add .` 后统一提交所有文件
- ❌ 禁止提交信息使用英文（description 部分）
- ❌ 禁止提交信息过于模糊（如 "fix bug"、"update"）
- ❌ 禁止将多个不相关的功能混入同一个提交