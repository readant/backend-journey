---
description: "测试规范：测试分层、单元测试结构、Mock 规范"
globs: ["**/*Test.java", "**/*Service.java", "**/*Controller.java"]
---

# 测试规范

## 1. 测试分层

```
        ╱╲
       ╱  ╲        E2E（少量）
      ╱────╲       完整业务流程
     ╱      ╲
    ╱────────╲      集成测试（适中）
   ╱          ╲     Spring 上下文 + 内存 DB
  ╱────────────╲
 ╱              ╲    单元测试（大量）
╱________________╲   纯逻辑验证，无外部依赖
```

## 2. 覆盖优先级

- ✅ **必须覆盖**：Service 层核心业务逻辑（含分支、异常、边界）
- ✅ **建议覆盖**：工具类、公共组件
- ⚠️ **可选**：简单 CRUD 的 Mapper
- ❌ **不需要**：Spring 配置类、Lombok 生成的方法

## 3. 测试框架

- JUnit 5（Spring Boot 3.x 默认）
- Mockito（Mock 外部依赖）
- AssertJ（流式断言）

## 4. 测试类结构

```java
@DisplayName("UserService 单元测试")
class UserServiceTest {

    @InjectMocks
    private UserService userService;

    @Mock
    private UserMapper userMapper;

    @Nested
    @DisplayName("getUserById 方法")
    class GetUserById {

        @Test
        @DisplayName("存在的用户ID → 返回用户信息")
        void shouldReturnUser_whenIdExists() {
            // given（准备数据）
            Long userId = 1L;
            UserEntity entity = new UserEntity().setId(userId).setUsername("test");
            when(userMapper.selectById(userId)).thenReturn(entity);

            // when（执行操作）
            UserVO result = userService.getUserById(userId);

            // then（验证结果）
            assertThat(result).isNotNull();
            assertThat(result.getUsername()).isEqualTo("test");
            verify(userMapper).selectById(userId);
        }

        @Test
        @DisplayName("不存在的用户ID → 抛出业务异常")
        void shouldThrowException_whenIdNotFound() {
            // given
            when(userMapper.selectById(999L)).thenReturn(null);

            // when & then
            assertThatThrownBy(() -> userService.getUserById(999L))
                .isInstanceOf(BusinessException.class)
                .hasMessageContaining("用户不存在");
        }
    }
}
```

## 5. 命名规范

- 测试类：`{被测类名}Test`，如 `UserServiceTest`
- 测试方法：`should{预期行为}_when{条件}`，如 `shouldReturnUser_whenIdExists`
- 使用 `@DisplayName` 写中文描述
