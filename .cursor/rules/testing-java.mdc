---
description: "测试分层 + 测试结构 + Mock 规范"
globs: ["**/*Test.java", "**/*Service.java", "**/*Controller.java"]
---

# 0. 测试分层：本规则管什么

| 测试类型 | 形态 | 归属 | 详细方法 |
| ---- | ---- | ---- | ---- |
| 单元测试 | 写 `*Test.java` 代码 | **本规则**（硬约束，写测试代码时触发） | `docs/testing/01~07` |
| 功能测试（接口冒烟 / UI 走查 / AI 联调） | 跑命令、点浏览器，不写代码 | 不在本规则内，按流程执行 | `docs/testing/08`（运行）/ `09`（AI 联调）/ `10`（变更后分层测） |

分工铁律：

- **单元测试是长期资产**：每次改动必跑 `mvn test` 全量防回归（74 用例秒级），核心 Service 必须配套
- **功能测试是一次性检查**：按影响面选端点测（改哪个端点测哪个），结果用完即弃，不留代码
- **两者互补不互相替代**：单测 Mock 掉了数据库和 Controller，验证不了分层组装；功能测试验证真实链路，但防不了回归

# 1. 覆盖优先级
✅ 必须覆盖：Service 层核心业务逻辑（分支/异常/边界）
✅ 建议覆盖：工具类、公共组件
⚠️  可选：简单 CRUD 的 Mapper
❌ 不需要：Spring 配置类、Lombok 生成的方法

# 2. 技术栈
- JUnit 5（@Test、@Nested、@DisplayName）
- Mockito（@Mock、@InjectMocks、when/verify）
- AssertJ（assertThat 流式断言）

# 3. 测试类结构（given-when-then）
```java
@DisplayName("UserService 单元测试")
class UserServiceTest {

    @InjectMocks
    private UserServiceImpl userService;

    @Mock
    private UserMapper userMapper;

    @Nested
    @DisplayName("getUserById")
    class GetUserById {

        @Test
        @DisplayName("存在的ID → 返回用户信息")
        void shouldReturnUser_whenIdExists() {
            // given
            Long userId = 1L;
            when(userMapper.selectById(userId)).thenReturn(new User().setId(userId));
            // when
            UserVO result = userService.getUserById(userId);
            // then
            assertThat(result).isNotNull();
            verify(userMapper).selectById(userId);
        }

        @Test
        @DisplayName("不存在的ID → 抛业务异常")
        void shouldThrowException_whenIdNotFound() {
            when(userMapper.selectById(999L)).thenReturn(null);
            assertThatThrownBy(() -> userService.getUserById(999L))
                .isInstanceOf(BusinessException.class)
                .hasMessageContaining("用户不存在");
        }
    }
}
```

# 4. 命名规范
- 测试类：{被测类名}Test（UserServiceTest）
- 测试方法：should{预期行为}_when{条件}
- @DisplayName 写中文描述

# 5. 经验沉淀
- 测试实战技巧与踩坑沉淀在 `docs/testing/`（心法→工具→实战→排查→工具链与协作，5 组 10 篇）
- 新踩的坑 → 补充 `docs/testing/07-反模式与常见坑.md`（现象→原因→解法）
- 新技巧 → 补充对应篇章，覆盖清单见 `docs/coverage/bj-01-cms-coverage.md`
